package entity.message;

public enum MessageType {
    LOGIN,
    LOGOUT,
    REGISTER,
    MATCH,
    CANCEL_MATCH,
    CREATE_ROOM,
    FIND_ROOM,
    OUT_ROOM,
    LEADERBOARD,
    PLAY,
    PLAY_ACTION,
    REFRESH,
}
